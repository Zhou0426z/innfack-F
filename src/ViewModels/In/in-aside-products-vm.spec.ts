import { InAsideProductsVM } from './in-aside-products-vm';

describe('InAsideProductsVM', () => {
  it('should create an instance', () => {
    expect(new InAsideProductsVM()).toBeTruthy();
  });
});
